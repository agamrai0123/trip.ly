# user-service — .env.example
# Copy to .env. Never commit .env.

PORT=8083
GRPC_PORT=9083

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wanderplan
DB_USER=postgres
DB_PASSWORD=changeme
DB_SSL_MODE=disable

# gRPC upstream — trip-service
TRIP_SERVICE_GRPC_ADDR=localhost:9082

# Rate limiting
RATE_LIMIT_RPS=1000
RATE_LIMIT_BURST=2000
